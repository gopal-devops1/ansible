- name: install and run nginx 
  hosts: 54.242.105.126
  become: yes 
  tasks:
    - name: install nginx package 
      ansible.builtin.package;
        name: nginx
        state: present
    - name: run nginx
      ansible.builtin.package;
        name: nginx
        state: started 